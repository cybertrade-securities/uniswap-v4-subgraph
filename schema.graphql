type Token @entity(immutable: false) {
  id: ID!
  symbol: String!
  name: String!
  decimals: BigInt!
}

type Pool @entity(immutable: false) {
  id: ID!
  token0: Token!
  token1: Token!
  createdAtBlockNumber: BigInt!
  hooks: String!
  tickSpacing: BigInt!
  feeTier: BigInt!
}
